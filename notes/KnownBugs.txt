----
2022.10.19
Extensions.cs, SCommon.cs ÇÃ ForEach

åÎÅF
	public static IEnumerable<T> ForEach<T>(this IEnumerable<T> src, Action<T> action)
	{
		foreach (T element in src)
			action(element);

		return src;
	}

ê≥ÅF
	public static IEnumerable<T> ForEach<T>(this IEnumerable<T> src, Action<T> action)
	{
		foreach (T element in src)
		{
			action(element);
			yield return element;
		}
	}

